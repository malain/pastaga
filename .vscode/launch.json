{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "smartStep": true,
            "name": "Debug",
            "program": "${workspaceFolder}/lib/index.js",
            "preLaunchTask": "tsc: build - tsconfig.json",
            "outFiles": [
                "${workspaceFolder}/lib/**/*.js"
            ],
            "args": [
                "init", "--template", "vulcainService", "--project", "test", "--outputFolder", ".", "--service", "test", "--domain", "test"
            ]
        }
    ]
}